packages:
  darwin:
    brews:
      # --- CLI ---
      - "mise"
      - "htop"
      - "watch"
      - "antidote"
      - "croc"

      # --- Encryption ---
      - "gnupg"

      # --- Git ---
      - "git"

      # --- LSPs, Formatters ---
      - "clang-format"
      - "nixfmt"
      - "helm-ls"

      # --- Docker ---
      - "colima"
      - "docker"
      - "docker-compose"
      - "docker-buildx"
      - "qemu"

      # --- AWS ---
      - "jaehong21/tap/hibiscus"

      # --- Media ---
      - yt-dlp
      - gallery-dl

    casks:
      - "wezterm"
      - "1password"
      - "1password-cli"
